# Self-Improvement QA

Release gates: migration/ORM parity, static safety scan, contract tests, fallback tests, shadow privacy boundary, approval evidence, canary health, and rollback rehearsal. Production promotion is disabled by default and must be guardian-enabled after review.
